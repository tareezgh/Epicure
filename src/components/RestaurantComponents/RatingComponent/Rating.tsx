import { useState } from "react";
import { useSelector } from "react-redux";
import { useNavigate } from "react-router";

import {} from "./style";

interface Params {
  page?: string;
}

const Rating = (ratingProps: Params) => {
  return <></>;
};

export default Rating;
