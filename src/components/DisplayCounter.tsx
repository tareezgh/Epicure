import { useSelector } from "react-redux";

export default function DisplayCounter() {
  // const count = useSelector((state : number)=> state.counter.value);

  return (
    <></>
    // <div>
    //     DisplayCounter in another component! <br />
    //     {count}
    // </div>
  );
}
